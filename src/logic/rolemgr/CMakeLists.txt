INCLUDE_DIRECTORIES(
	"${PROJECT_SOURCE_DIR}/src/public"
	"${PROJECT_SOURCE_DIR}/src/kernel/api"
	"${PROJECT_SOURCE_DIR}/src/logic/interface"
	"${PROJECT_SOURCE_DIR}/src/logic/define"
	"${PROJECT_SOURCE_DIR}/src/sllib/api"
)

SET(ROLEMGR
	"${PROJECT_SOURCE_DIR}/src/logic/rolemgr/Role.h"
	"${PROJECT_SOURCE_DIR}/src/logic/rolemgr/Role.cpp"
	"${PROJECT_SOURCE_DIR}/src/logic/rolemgr/RoleMgr.h"
	"${PROJECT_SOURCE_DIR}/src/logic/rolemgr/RoleMgr.cpp"
	"${PROJECT_SOURCE_DIR}/src/logic/rolemgr/main.cpp"
)

SOURCE_GROUP(api FILES ${SL_KERNEL_API})
SOURCE_GROUP(public FILES ${PUBLIC})
SOURCE_GROUP(interface FILES ${SL_LOGIC_INTERFACE})
SOURCE_GROUP(define FILES ${SL_LOGIC_DEFINE})
SOURCE_GROUP(\\ FILES ${ROLEMGR})

ADD_LIBRARY(RoleMgr SHARED
	${ROLEMGR}
	${SL_KERNEL_API}
	${PUBLIC}
	${SL_LOGIC_INTERFACE}
	${SL_LOGIC_DEFINE}
)

IF(WIN32)
TARGET_LINK_LIBRARIES(RoleMgr AttrGetter.lib)
ENDIF(WIN32)

IF(UNIX)
TARGET_LINK_LIBRARIES(RoleMgr libAttrGetter.so)
ENDIF(UNIX)

#SET_PROPERTY(TARGET Gate PROPERTY FOLDER "core")
SET_PROPERTY(TARGET RoleMgr PROPERTY FOLDER "framework")
