INCLUDE_DIRECTORIES(
	"${PROJECT_SOURCE_DIR}/src/public"
	"${PROJECT_SOURCE_DIR}/src/kernel/api"
	"${PROJECT_SOURCE_DIR}/src/logic/interface"
	"${PROJECT_SOURCE_DIR}/src/logic/define"
	"${PROJECT_SOURCE_DIR}/src/sllib/api"
)

SET(PLAYERMGR
	"${PROJECT_SOURCE_DIR}/src/logic/playermgr/PlayerMgr.h"
	"${PROJECT_SOURCE_DIR}/src/logic/playermgr/PlayerMgr.cpp"
	"${PROJECT_SOURCE_DIR}/src/logic/playermgr/main.cpp"
)

SOURCE_GROUP(api FILES ${SL_KERNEL_API})
SOURCE_GROUP(public FILES ${PUBLIC})
SOURCE_GROUP(interface FILES ${SL_LOGIC_INTERFACE})
SOURCE_GROUP(define FILES ${SL_LOGIC_DEFINE})
SOURCE_GROUP(\\ FILES ${PLAYERMGR})

ADD_LIBRARY(PlayerMgr SHARED
	${PLAYERMGR}
	${SL_KERNEL_API}
	${PUBLIC}
	${SL_LOGIC_INTERFACE}
	${SL_LOGIC_DEFINE}
)

TARGET_LINK_LIBRARIES(PlayerMgr
	${SL_LIB_DEBUG}
	${SL_LIB_RELEASE}
	${ATTR_LIBD}
	${ATTR_LIB}
)
TARGET_LINK_LIBRARIES(PlayerMgr "AttrGetter.lib")

#SET_PROPERTY(TARGET Gate PROPERTY FOLDER "core")
SET_PROPERTY(TARGET PlayerMgr PROPERTY FOLDER "framework")